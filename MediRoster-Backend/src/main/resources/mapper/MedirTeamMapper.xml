<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mediroster.mapper.MedirTeamMapper">

    <resultMap id="BaseResultMap" type="com.mediroster.entity.MedirTeam">
        <id column="id" property="id"/>
        <result column="team_code" property="teamCode"/>
        <result column="team_name" property="teamName"/>
        <result column="description" property="description"/>
        <result column="enabled" property="enabled"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO medir_team (team_code, team_name, description, enabled)
        VALUES (#{teamCode}, #{teamName}, #{description}, #{enabled})
    </insert>

    <update id="updateById">
        UPDATE medir_team
        SET team_code = #{teamCode},
            team_name = #{teamName},
            description = #{description},
            enabled = #{enabled}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM medir_team WHERE id = #{id}
    </delete>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM medir_team WHERE id = #{id}
    </select>

    <select id="findByTeamCode" resultMap="BaseResultMap">
        SELECT * FROM medir_team WHERE team_code = #{teamCode}
    </select>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT * FROM medir_team ORDER BY id
    </select>
</mapper>
